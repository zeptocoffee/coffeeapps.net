{
  "name": "coffeeapps.net",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "serve": "npx @11ty/eleventy --serve & npx tailwindcss -c ./tailwind.config.js -i styles/tailwind.css -o _site/tailwind.min.css --watch",
    "build": "npx @11ty/eleventy && npx tailwindcss -c ./tailwind.config.js -i styles/tailwind.css -o _site/tailwind.min.css --minify"
  },
  "dependencies": {
    "@11ty/eleventy": "^0.12.1",
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.4",
    "tailwindcss": "^3.0.0"
  },
  "devDependencies": {}
}
